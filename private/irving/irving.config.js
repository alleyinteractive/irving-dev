import styledComponentsConfig from '@irvingjs/styled';
import loadableConfig from '@irvingjs/loadable';

const config = {
  name: 'irving-dev-app',
  packages: [
    styledComponentsConfig,
    loadableConfig,
  ],
};

export default config;
